# 闭卷限时卷（Day50–60 各一题）

> 规则：每题限时 8 分钟，**闭卷**，不许翻 study、不许搜。答不出的当场写「不会」，
> 回填对应 Day 后再重考。「看过」不计入掌握。
> 判卷要点在 `closed_book_rubric.md`，**作答前不要打开**。

---

## Q1 · Java（Day50）
写出 `? extends` 与 `? super` 的合法读写示例，并解释 PECS。
在 `List<? extends Number>` 上调用 `add(1)` 会发生什么？为什么？

作答：

```
（此处作答）
```

## Q2 · Spring（Day51）
self-invocation（同类内部方法互调）为什么会让 `@Transactional` 失效？
给出两种修复方式，并说明各自的代价。

作答：

```
（此处作答）
```

## Q3 · MySQL（Day52）
在 RC 与 RR 下，同一事务内两次执行 `SELECT balance FROM t WHERE id=1` 结果可能有什么差异？
`SELECT ... FOR UPDATE` 加的是什么锁？什么时候释放？

作答：

```
（此处作答）
```

## Q4 · Redis / MQ（Day53）
cache-aside 模式下，写操作应该「先更新数据库再删缓存」还是「先删缓存再更新数据库」？
分别说明另一种顺序会出现什么问题。Outbox 解决的是什么问题？

作答：

```
（此处作答）
```

## Q5 · 安全（Day54）
JWT 的校验顺序应该是怎样的（至少 4 步）？
IDOR（越权访问他人资源）应该在哪一层修？给出本项目里对应的位置。

作答：

```
（此处作答）
```

## Q6 · Linux / 部署（Day55）
用户反馈「接口很慢」，请列出分层排查命令：DNS → TCP → TLS → 服务端 → 数据库，每层一条。

作答：

```
（此处作答）
```

## Q7 · JVM / 性能（Day56）
线上 CPU 突然飙高，请给出从现象到结论的证据链（用什么命令、看什么指标、怎么定位到代码行）。

作答：

```
（此处作答）
```

## Q8 · 系统设计（Day57）
深分页 `LIMIT 100000, 20` 为什么慢？keyset（游标）分页怎么做？
为什么必须加唯一 tiebreaker？

作答：

```
（此处作答）
```

## Q9 · 微服务（Day58）
两层重试（网关重试 × 服务间重试）会把下游压力放大多少倍？
如何控制（说出至少两种手段）？

作答：

```
（此处作答）
```

## Q10 · 分布式（Day59）
用了 Redis 分布式锁，为什么还要数据库唯一键或 fencing token？
举一个 Redis 锁失效导致数据被破坏的具体时序。

作答：

```
（此处作答）
```

## Q11 · Elasticsearch（Day60）
`text` 与 `keyword` 的区别是什么？
深分页用 `from/size` 有什么问题，`search_after` 如何解决？

作答：

```
（此处作答）
```

---

## 自评表

| 题号 | 是否答出 | 卡在哪一层（是什么 / 为什么 / 项目里怎样） | 回填 Day |
|---|---|---|---|
| Q1 | | | |
| Q2 | | | |
| Q3 | | | |
| Q4 | | | |
| Q5 | | | |
| Q6 | | | |
| Q7 | | | |
| Q8 | | | |
| Q9 | | | |
| Q10 | | | |
| Q11 | | | |

能连续扛住三层追问（是什么 → 为什么 → 项目里怎样 / 反例）才算掌握。
