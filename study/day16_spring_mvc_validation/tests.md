# 必须会背会写的验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 测空白/超长/错误 JSON
- 断言 code/requestId/timestamp
- 写出 `@RequestBody`、`@PathVariable`、`@RequestHeader` 和 `@Valid` 的 Controller 方法。
- 写出嵌套 `@Valid` DTO，并区分字段校验、Service 校验和错误处理器的职责。

# 额外测试与追问

- 测未知字段、空数组、重复 query 参数
