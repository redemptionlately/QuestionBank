# 必须会背会写的验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 画状态图
- 测非法转换 409
- 写出候选状态、版本状态、审核记录字段、版本检查和追加审计规则。
- 写出 `fromState + command + actor -> toState` 状态转换表。
- 构造旧页面覆盖新审核决定的并发冲突。

# 额外测试与追问

- 设计双审核并发
- 审计每次转换
