# 上半部分：当天会背会写验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 写出第一次提交、相同 key 重试、不同 key 重试三条分支。
- 缺少 `Idempotency-Key` 返回 400。
- 相同 key 重试返回相同总分和逐题结果。
- 相同 key 重试不重复增加错题次数。
- 不同 key 重试返回 409，提交后保存答案返回 409。

# 下半部分：额外测试

- 同一会话并发发送两个相同 key 请求，检查最终只有一个结果。
- 同一会话并发发送两个不同 key 请求，记录哪个请求成功、哪个冲突。
- 解释把同步锁去掉后，哪些数据库写入可能重复。

