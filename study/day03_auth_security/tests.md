# 上半部分：当天会背会写验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 写出密码摘要校验、令牌签发和 Bearer 过滤器的核心流程。
- 正确管理员登录返回 200、token 和 `ADMIN`。
- 错误密码、禁用用户、缺少 token 分别验证 401。
- 学生访问管理员接口、管理员访问学生接口分别验证 403。
- 解释认证与授权的区别。

# 下半部分：额外测试

- 修改 token 一个字符后请求接口，确认不能冒充用户。
- 等待或模拟过期 token，确认过滤器清理过期记录。
- 检查响应和日志中没有密码、完整 token 或内部堆栈。

