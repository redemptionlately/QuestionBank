# Current
- 打开并按当天 study.md 的“源码索引（MustRemember）”或“外部源码索引（MustRemember）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 比较 JPA 与 MyBatis 的职责、优点和当前选择理由。
- 说出 M0、M1、M2、M3 的边界，不把未来计划说成已实现。
- 写出一次引入 Redis 或 Agent 前必须回答的四个问题：具体问题、现有方案不足、失败降级、测试方法。
- 给出一个 M0 代码小变式并说明需要修改的迁移、Service 和测试。
- 为草稿创建、版本发布、练习提交写出资源状态转换和模块调用方向。

# External

- 将判分规则改为多选部分得分，列出数据模型、事务和兼容性影响，不直接实现。
- 将内存 token 替换为持久会话，画出表结构、撤销流程和重启行为。
- 写一页 M0 复盘：事实、推断、限制、下一步，不写未经测试的性能结论。
