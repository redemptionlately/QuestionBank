# 上半部分：当天会背会写验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 写出启动 MySQL、查看健康状态、停止服务的 Compose 命令。
- MySQL healthcheck 通过后启动 Java 应用。
- 应用首次启动成功执行 Flyway V1。
- 重启应用不重复执行 V1，已发布数据仍存在。
- 解释环境变量覆盖数据库连接配置的方式。

# 下半部分：额外测试

- 停止 MySQL 后请求健康检查和业务接口，比较进程存活与依赖就绪。
- 删除容器但保留 volume，确认数据恢复；明确删除 volume 的不可逆风险。
- 使用错误数据库密码启动，记录失败阶段和错误信息。

