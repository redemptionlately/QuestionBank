# Current
- 打开并按当天 study.md 的“源码索引（MustRemember）”或“外部源码索引（MustRemember）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 写并验证 TopN
- 定位慢查询
- 写出窗口函数、CTE、EXISTS、MVCC、gap lock 和死锁检测的 SQL/并发示例。
- 写出 Top-N per group 的 ROW_NUMBER SQL。
- 给出函数包列、隐式转换和循环锁顺序导致的三个问题。

# External

- 比较窗口函数/相关子查询
- 测 NULL/时区
