# 必须会背会写的验收

- 写出 JDBC 查询的资源关闭和参数绑定代码；写出 MyBatis `#{}` 与 `${}` 的对照。
- 给定连接池、线程池和数据库连接上限，计算安全并发边界并说明假设。
- 为题库发布和练习提交分别选择隔离级别、索引和锁策略，解释可能的死锁路径。

# 额外测试与追问

- 为一个 Entity 关联查询构造 N+1，分别用批量查询和投影修复并比较 SQL 数量。
- 解释 READ COMMITTED 与 REPEATABLE READ 下两次查询结果差异。
- 看到 `EXPLAIN` 的全表扫描、回表和 filesort 时给出验证顺序。
