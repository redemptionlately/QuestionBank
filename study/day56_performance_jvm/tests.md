# 必须会背会写的验收

- 写出一次 Java 服务从 CPU 高、延迟高到使用 `jcmd/jstack/JFR` 定位的证据链。
- 根据 CPU 密集、阻塞 I/O 和混合任务选择线程池参数、队列和拒绝策略。
- 写出堆、Metaspace、直接内存和线程栈溢出的症状与首个证据。

# 额外测试与追问

- 比较公共 ForkJoinPool、自定义 Executor 和虚拟线程处理阻塞任务的风险。
- 解释 P95 上升但平均值稳定时可能的排队、GC 或锁竞争原因。
- 设计一次只改变线程池队列容量的基准实验并列出停止条件。
