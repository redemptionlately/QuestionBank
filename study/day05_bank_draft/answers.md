# Current
- 打开并按当天 study.md 的“源码索引（MustRemember）”或“外部源码索引（MustRemember）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 写出题库创建、草稿版本创建和题目校验的 Service 流程。
- 管理员创建题库和含单选/多选/判断题的草稿成功。
- 空题干、空选项、标准答案不属于选项分别返回 400。
- 学生创建题库、非所有者编辑题库分别返回 403。
- 解释为什么版本号和题号需要数据库唯一约束。

# External

- 两个版本并发创建，检查是否出现重复版本号。
- 尝试使用客户端提供的分数，确认 API 不接受该字段作为事实。
- 查询旧版本，确认创建新版本不会修改旧题。

