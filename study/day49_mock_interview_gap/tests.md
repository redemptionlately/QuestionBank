# Current
- 打开并按当天 study.md 的“源码索引（MustRemember）”或“外部源码索引（MustRemember）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 60分钟模拟并运行 test/package/E2E
- 列五个不会
- 分别口述对象模型、泛型、集合、异常、I/O、JUC、JMM、JVM、Spring、MySQL、HTTP 和算法核心概念。
- 写出 `HashMap` 定位/扩容、`volatile` 与 `synchronized`、JVM 内存区和 `try-with-resources` 示例。
- 对一个项目问题给出业务目标、数据流、事务/锁、数据库事实、错误响应和证据边界。
- 区分 Java 语义、Spring 代理、SQL 执行计划和并发共享状态问题的回答入口。
- 把一个不会的问题拆成定义、源码位置、边界、证据和复习缺口五项。
- 对 Spring AOP、JDBC/MyBatis、消息队列、TCP/Linux、API 分页和容器探针各回答一个定义、代码/命令、失败边界和项目结合问题。
- 写出一次安全的 Git 分支协作和最小 CI 流水线，并用 M0 判分逻辑说明 Strategy 与事件通知的适用边界。
- 用 Java 8 Stream 和 Optional 写一个无副作用的数据转换链，并解释空值分支。
- 写出 `Path/Files` 文件读写和 JSON 字段兼容的示例。
- 设计一个 Spring Boot 配置、数据库迁移和容器滚动发布的回滚方案。

# External

- 按 JD 调整 Java 后端优先级
- 追问通过才 learned
- 解释 Git `merge`、`rebase`、`cherry-pick` 的提交图变化和冲突恢复风险。
- 写出编译、测试、静态检查、镜像、发布、回滚的 CI 顺序，并指出密钥注入位置。
- 为 M0 判分和发布各选择一个设计模式，写出模式解决的问题和不应使用的场景。
