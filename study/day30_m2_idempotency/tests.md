# 必须会背会写的验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 画两个实例抢 key
- 设计摘要冲突
- 写出 `scope`、业务 key、request hash、状态、响应和过期时间的幂等记录模型。
- 写出幂等表 DDL 和唯一键竞争后的两条事务分支。
- 设计 PROCESSING 超时接管和过期清理边界。

# 额外测试与追问

- 比较 SETNX/MySQL 唯一键
- 设计清理
