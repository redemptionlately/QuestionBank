# Current（复述答案要点，写你自己的话；对答案标准见 solutions.md）

- 网关/发现/超时/熔断/降级配置复述：网关只做验签+透传+限流+Request-Id；Eureka 注册-续约-剔除三要素；Feign 超时为什么必须显式配；熔断三状态与触发条件（窗口 5、失败率 50%）；"快速失败"与"兜底数据"两种降级各自适用什么接口。
- Feign 幂等复述：幂等键叫什么（`clientToken`）、存在哪（`uk_practice_session_token` 唯一键）、为什么幂等查询要放在远程调用之前；哪些异常不重试（400/401/403 与非幂等写）。
- 全链路图复述：默写链路并标注每步失败码——无 token 401 / 伪造 401 / 限流 429 / auth 挂 503 / 并发发布撞唯一键 409；`X-Request-Id` 在哪生成、为什么 401 响应也要带。

# External

- 单体 vs 拆分复述：本拆分为什么"发布事务"仍是单个本地事务（数据所有权划对了，强一致留在服务内）；跨服务只剩 outbox 至少一次 + 消费端 `event_key` 幂等。
- 配置中心复述：当前**未接入**，哪些配置将来必须外置（DB 地址/token 密钥/限流阈值）、`/actuator/refresh` 生效范围、密钥为什么仍走环境变量。
- 重试放大复述：3×2=6 倍的算法、为什么调用链零 retry 只留 relay 下轮重试、限流是第一道闸。
- 数据所有权复述：`SHOW TABLES` 三库互不相交；`practice_item` 固化快照副本是"跨服务 JOIN 的替代品"；bank 挂了为什么已开会话仍能交卷。
