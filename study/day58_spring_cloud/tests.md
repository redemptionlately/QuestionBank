# 必须会背会写的验收

- 为题库查询和导入服务设计网关、服务发现、超时、重试、熔断和降级配置。
- 写出 OpenFeign 调用的错误映射和幂等条件，指出哪些异常不能重试。
- 画出一次请求经过网关、服务发现、负载均衡、下游服务和数据库的链路。

# 额外测试与追问

- 解释为什么 M0 当前不拆微服务，以及拆分后题库发布事务如何变化。
- 设计配置中心回滚、实例摘除和下游雪崩的恢复路径。
- 计算两层重试在峰值流量下的放大倍数并设置上限。
