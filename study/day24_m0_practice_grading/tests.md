# 必须会背会写的验收
- 打开并按当天 study.md 的“源码索引（会背会写）”或“外部源码索引（会背会写）”逐项定位文件、类/方法、SQL 对象和行号，独立写出对应代码或 SQL，并口述输入、输出与边界。

- 运行三题型测试
- 提交后 PUT 409
- 写出缺答使用 `[]`、多选排序规范化、`totalScore`/`maxScore` 计算和 Session/Item 持久化规则。
- 写出 `SubmitResult` 与 `GradedAnswer` record，并说明 Session、Item、WrongQuestion 的写入关系。

# 额外测试与追问

- 增加重复答案规则
- 说明主观题不进 M0
